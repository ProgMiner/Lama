var x, y, z;

fun sum (x) {
  case x of
    Nil          ->  0
  | Cons (x, tl) ->  (x + sum (tl))
  esac
}

x := read ();

write (sum (Nil));

y := Nil ;
y := Cons (100, y) ;
write (sum (y));

z := Nil ;
z := Cons (200, z) ;
z := Cons (100, z) ;
write (sum (z))
